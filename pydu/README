A simple "disk usage" script with a visually more attractive/useful display
than the standard "du" utility.

Use "pydu.py --help" to see how it works. But it should be straight-forward

LICENSE: LGPL

